[project]
name = "dify-prompt-template"
version = "0.1.0"
description = "Render prompt templates with workflow variables — just like the LLM node prompt editor, but outputs a string."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.9.0",
]

[dependency-groups]
dev = []
